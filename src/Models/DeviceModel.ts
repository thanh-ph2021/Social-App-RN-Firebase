export interface DeviceModel {
    deviceID: string,
    deviceName: string,
    deviceBrand: string,
    deviceOS: string,
    deviceOSVersion: string,
    lastLogin?: any,
    notifyToken: string,
} 